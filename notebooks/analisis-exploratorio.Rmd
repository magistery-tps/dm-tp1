---
title: 'TP I: Analisis Exploratorio'
date: "2021-05-01"
output:
  html_document:
    highlight: tango
    theme: sandstone
    df_print: paged
---

# Analisis Exploratorio

## Analisis univariado

Importamos la librerias necesarias
```{r}
library(pacman)
p_load(stringi, tidyverse, this.path, readr)
source(paste(this.path::this.dir(), '/../lib/import.R', sep=''))

import('../lib/data-access')
import('../lib/plot')
```

Veamos las distribuciones de cada variable numérica:

### Danceability

```{r}
get_collection_fields <- function(collection_name, field_name) {
  collection <- get_collection(collection_name)
  projection <- paste('{ "', field_name, '": 1 }', sep='')
  collection$find(fields = projection)
}

result <- get_collection_fields('track_real_features.num', 'danceability')
```

```{r}
p <- gplot_hist(result$danceability, name="Indice de Vailabilidad", binwidth=0.06)
ggplotly(p)
```








